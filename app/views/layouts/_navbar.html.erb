<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "SHOOT IT!", root_path, class: "navbar-brand float-left font-weight-bold" %>  
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

   
    <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to  "Salir", destroy_user_session_path, method: :delete, class: "nav-link fixed-right" %>
        </li>  
        <% else %>
        <li class="nav-item">
          <%= link_to  "Inicia sesión", new_user_session_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to  "Regístrate", new_user_registration_path, class: "nav-link" %>
        </li>
        <% end %>
      <li class="nav-item">
       <span class="nav-link"> <%= current_user.email if user_signed_in? %> </span>
      </li>
    </ul>
  
</nav>